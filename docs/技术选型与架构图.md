## 一、技术选型决策

### 1.1 桌面框架选型

| 方案 | 优势 | 劣势 | 结论 |
|------|------|------|------|
| **Electron** | Web 技术复用、生态丰富、3D/可视化支持好 | 体积大（~80MB）、内存占用较高 | ✅ 选用 |
| Tauri | 体积小（~5MB）、Rust 后端安全 | WebView 兼容性差、ECharts GL 支持未验证 | ❌ |
| Qt/C++ | 原生性能、体积可控 | 开发效率低、前端生态无法复用 | ❌ |

**决策理由**：项目核心是 3D 可视化 + AI 对话的富交互演示，Electron 的 Chromium 内核确保 WebGL/ECharts GL 完整支持，且团队 Web 技术栈可直接复用。

### 1.2 AI 服务选型

| 方案 | 优势 | 劣势 | 结论 |
|------|------|------|------|
| **Anthropic Claude** | Extended Thinking 能力强、流式输出、上下文窗口大 | 需海外 API/中转 | ✅ 选用 |
| OpenAI GPT-4 | 生态最广、Tool Use 成熟 | 国内访问受限 | 备选 |
| 本地 LLM (Ollama) | 完全离线、无 API 费用 | 推理质量不足、部署复杂 | 未来规划 |

**决策理由**：Claude 的 Extended Thinking 适合需要推理链的工业分析场景。通过 `baseURL` 配置支持第三方中转，兼容国内部署。离线 Mock 确保无 API 时可演示。

### 1.3 可视化选型

| 方案 | 优势 | 劣势 | 结论 |
|------|------|------|------|
| **ECharts + ECharts GL** | 图表类型丰富、3D 支持、中文文档 | 包体较大（~4MB） | ✅ 选用 |
| D3.js | 灵活度高、定制能力强 | 开发成本高、无内置 3D | ❌ |
| Three.js | 3D 能力最强 | 2D 图表需另选方案 | ❌ |
| Plotly | 统计图表方便 | 体积大、定制困难 | ❌ |

**决策理由**：ECharts 2D + GL 3D 统一生态，减少学习和集成成本。丰富的图表类型（热力图、仪表盘、力导向图、3D 散点）满足全部可视化需求。

### 1.4 构建工具选型

| 方案 | 优势 | 劣势 | 结论 |
|------|------|------|------|
| **electron-vite** | 原生支持 Electron 多入口、Vite HMR 极速 | 相对新 | ✅ 选用 |
| electron-forge + webpack | 官方推荐、成熟 | 配置复杂、HMR 慢 | ❌ |
| vite + 手动配置 | Vite 快 | 需手动处理 main/preload/renderer | ❌ |

---

## 二、V1 版架构图（简化版）

适用于快速理解系统核心组件和数据流向。

```mermaid
graph LR
    UI["React 可视化界面"]
    AI["AI 运维助手"]
    ENGINE["TADPE 预测引擎"]
    DATA["数据仿真层"]
    LLM["Claude API / 第三方中转"]
    EXT["浏览器插件"]

    DATA --> UI
    DATA --> AI
    ENGINE --> UI
    AI <-->|"streaming"| LLM
    EXT -->|"HTTP"| UI
```

**说明**：
- 数据仿真层提供气缸设备的全量 Mock 数据
- AI 助手在线时调用 Claude API（支持中转），离线时使用本地预置回答
- TADPE 引擎在前端模拟运行，展示算法推理过程
- 浏览器插件通过本地 HTTP 将外部信息推送至应用

---

## 三、V2 版架构图（完整版）

展示完整模块关系、外部依赖、安全边界与扩展点。

```mermaid
graph TB
    subgraph EXT_SVC["外部服务"]
        CLAUDE["Anthropic Claude API"]
        RELAY["第三方 LLM 中转"]
        BROWSER["Chrome/Edge 浏览器"]
    end

    subgraph MAIN_P["主进程 Main Process"]
        ENTRY["index.ts 窗口管理"]
        IPC_H["ipc.ts IPC Handler + Zod"]
        CLIENT["anthropicClient.ts 流式推理"]
        PROMPT["dashboardPrompt.ts Prompt"]
        MOCK_D["mockData.ts 仿真数据"]
        ANALYTICS["analytics.ts KPI计算"]
        HTTP_S["Bridge Server :17654"]
    end

    subgraph PRELOAD_P["预加载层 Preload"]
        API["window.predMaint API"]
    end

    subgraph RENDER_P["渲染进程 Renderer"]
        APP_R["App.tsx 视图路由"]
        V_DASH["Dashboard 仪表盘"]
        V_ALGO["Algorithm TADPE引擎"]
        V_BIG["BigScreen 3D大屏"]
        V_SEC["Security 网络态势"]
        EC_2D["ECharts 2D 图表"]
        EC_3D["ECharts GL 3D"]
        CHAT_P["ChatPanel Markdown"]
    end

    subgraph SEC_TOOLS["Python 安全工具"]
        SCAN["scanner.py"]
        CYBER["cybereye.py"]
        SHIELD["shieldscan.py"]
    end

    subgraph EXTENSION["浏览器插件 MV3"]
        POPUP["popup.html"]
        BG["background.js"]
        CONTENT["content.js"]
    end

    CLAUDE --> CLIENT
    RELAY --> CLIENT
    CLIENT --> PROMPT
    PROMPT --> ANALYTICS

    ENTRY --> IPC_H
    IPC_H --> CLIENT
    IPC_H --> MOCK_D
    MOCK_D --> ANALYTICS

    API --> IPC_H
    APP_R --> API

    APP_R --> V_DASH
    APP_R --> V_ALGO
    APP_R --> V_BIG
    APP_R --> V_SEC

    V_DASH --> EC_2D
    V_DASH --> CHAT_P
    V_ALGO --> EC_2D
    V_BIG --> EC_3D
    V_SEC --> EC_2D

    BROWSER --> CONTENT
    CONTENT --> BG
    BG --> POPUP
    POPUP -->|"HTTP POST"| HTTP_S
    HTTP_S --> IPC_H

    V_SEC -.->|"spawn"| SCAN
    V_SEC -.->|"spawn"| CYBER
    V_SEC -.->|"spawn"| SHIELD
```

---

### 安全边界说明

```mermaid
graph LR
    subgraph TRUST["信任边界内"]
        MAIN["Main Process"]
        PRE["Preload Context Bridge"]
    end

    subgraph UNTRUST["不信任区域"]
        RENDERER["Renderer 无Node访问"]
        EXTENS["Browser Extension"]
        NETWORK["外部网络"]
    end

    MAIN -->|"受控API"| PRE
    PRE -->|"白名单方法"| RENDERER
    EXTENS -->|"Token鉴权"| MAIN
    NETWORK -->|"TLS"| MAIN
```

---

### 数据流架构

```mermaid
flowchart TD
    subgraph SOURCE["数据源层"]
        MOCK["Mock数据生成器 6台气缸x36条时序"]
        REAL["真实数据接口 规划中"]
    end

    subgraph COMPUTE["计算层"]
        KPI_C["KPI聚合计算"]
        TREND_C["趋势分析"]
        ALERT_C["告警判定"]
        TADPE_C["TADPE推理引擎"]
    end

    subgraph PRESENT["呈现层"]
        CHART["ECharts图表"]
        TABLE["数据表格"]
        AI_OUT["AI报告生成"]
        THREE_D["3D数字孪生"]
    end

    MOCK --> KPI_C
    MOCK --> TREND_C
    MOCK --> ALERT_C
    MOCK --> TADPE_C
    REAL -.-> KPI_C

    KPI_C --> CHART
    TREND_C --> CHART
    ALERT_C --> TABLE
    TADPE_C --> CHART
    TADPE_C --> AI_OUT
    KPI_C --> THREE_D
```

---

## 四、部署架构（生产规划）

```mermaid
graph TB
    subgraph FIELD["产线现场"]
        PLC["PLC控制器"]
        SENSOR["传感器 位移/压力/温度"]
        GW["5G工业网关"]
    end

    subgraph EDGE["5G MEC 边缘"]
        GNB["5G gNB基站"]
        MEC["MEC服务器 数据预处理"]
    end

    subgraph INTRA["企业内网"]
        DB[("TimescaleDB 时序数据库")]
        MQTT["EMQX 消息中间件"]
        APP_S["Aura-PHM 桌面端"]
        LLM_P["LLM代理网关"]
    end

    subgraph CLOUD["云端 可选"]
        CLAUDE_C["Claude API"]
    end

    SENSOR --> PLC
    PLC --> GW
    GW -->|"5G NR"| GNB
    GNB --> MEC
    MEC --> MQTT
    MQTT --> DB
    DB --> APP_S
    APP_S --> LLM_P
    LLM_P --> CLAUDE_C
```

---

## 五、IPC 通信时序图

```mermaid
sequenceDiagram
    participant R as Renderer
    participant P as Preload
    participant M as Main Process
    participant C as Claude API

    R->>P: predMaint.chat.send(text)
    P->>M: ipcRenderer.invoke chat:send
    M->>M: Zod schema 校验
    alt 在线模式
        M->>C: messages.stream()
        C-->>M: text delta SSE
        M-->>R: chat:delta event
        C-->>M: final message
        M-->>R: chat:done event
    else 离线模式
        M->>M: getMockChunks(prompt)
        M-->>R: setTimeout chat:delta
        M-->>R: chat:done offline=true
    end
```

---

*文档版本：v1.1 | 编制日期：2026-06-10*
